Contributing

Thanks for your interest! This project follows the strict rules in `ai-rules.md`.

- All changes must be accompanied by tests (unit + integration where applicable).
- Pin dependencies and update `manifest.txt` when upgrading tool versions.
- Run `make test` before opening a pull request.
- Follow the repo's coding standards and provide a clear changelog entry for each PR.